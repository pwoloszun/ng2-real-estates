import {
  Component, EventEmitter
} from '@angular/core';

import { SimpleGMap } from "./shared/simple-gmap";
import { Marker } from "./shared/marker";
import { LatLng } from "./shared/lat-lng";

// TODO 1: init map
// define property: @ViewChild('mapCont') mapContainer;
// later: gmap = new SimpleGMap(this.mapContainer.nativeElement);

// TODO 2: create markers
// let marker: Marker = gmap.createMarker(objWithLatLng);

// TODO 3: handle on marker click event
// marker.on("click", function(objWithLatLng) {});
