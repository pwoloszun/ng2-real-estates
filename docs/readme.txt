1) Installation
Open terminal/console, go to project directory, run:
$ npm install


2) Run app on localhost
In terminal, from project directory, run:
$ npm start

then in browser, open http://localhost:4200

3) Run Unit Tests
In terminal, from project directory, run:
$ npm test
